# Emulator UI for a PIC emulator written in Dart

The goal is to visualise the workings of the emulator using Flutter.

## Running

It depends on the `pic_dart_emu` package being available locally and pulled in with `pub`.